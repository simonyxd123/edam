# EDAM Worker 测试依赖
# 运行：cd worker && pytest

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.2  # 用于 HTTP 测试
fakeredis==2.24.1  # Redis mock
aioresponses==0.7.7  # 异步 HTTP mock